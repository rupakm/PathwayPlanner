# Adenylate Kinase (Paper 1 target)

Placeholder for the WP1/WP2 prototype: the initial action vocabulary
(`open_hinge(LID)`, `weaken_interface(LID, CORE)`, `rotate_domain(LID)`,
`relax()`, ...) grounded in AdK's CORE/LID/NMP domain structure, with
competing recipe orderings (weaken-then-open vs open-then-weaken) as the
first mechanistic-programming experiment.

Inputs to add: starting structures (1AKE closed / 4AKE open), force field
choice, domain/hinge residue definitions, backend configuration.
